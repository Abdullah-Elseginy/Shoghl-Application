import {View} from 'react-native';
import React from 'react';
import {AppScreenContainer, CustomText} from '../../components';

const ComplateSearchedCv = () => {
  return (
    <AppScreenContainer>
      <CustomText text="Sreach" />
    </AppScreenContainer>
  );
};

export default ComplateSearchedCv;
